internal class Program
{
    private static void Main(string[] args)
    {
        //Data Types ::
        int age = 32;
        string name = "Saida";
        byte[] data = { 1, 2, 3, 4, 5, 6, 7, 8 };
        decimal length = 1.2M;
        double height = 5.5;
        float rainDancetty = 2.3F;

        //Operators :: +,-,/,*,<,>,<=,>=
        // Incremant - ++
        // Decrement - --

        age = age + 2;
        Console.WriteLine(age;

        name = name + "Shaik";
        Console.WriteLine(name);

        length = length + 5;
        Console.WriteLine(length);

        age = age * 4;
        Console.WriteLine(age);

        age = age / 2;
        Console.WriteLine(age);
    }
}